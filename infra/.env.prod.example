# Production env for docker-compose.prod.yml. Copy to /opt/apexai/.env and fill secrets.
DOMAIN=apex-ai.clique-vpn.ru
ACME_EMAIL=you@example.com

# Generate strong values, e.g.:  openssl rand -hex 24
POSTGRES_PASSWORD=
JWT_SECRET=
MINIO_ROOT_USER=apexai
MINIO_ROOT_PASSWORD=
BILLING_WEBHOOK_SECRET=
